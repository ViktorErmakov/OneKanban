---
description: Инструкция для ИИ — OneKanban, связка 1С/HTML, YAXUnit+Playwright тесты, v8std, EDT+MCP, документация в репо
alwaysApply: true
---

# Инструкция для модели: Kanban_for_1C / OneKanban

**Назначение файла:** быстро включиться в проект и действовать правильно. Это не текст для конечного пользователя.

## Продукт и репозиторий

- Расширение 1С: канбан в **поле HTML-документа**. Фронт собирается в **`OneKanban-html`** → один инлайн-HTML попадает в макеты обработки **`Канбан_Доска_HTML`** (`ШаблонКанбанДоскаHTML`, пустой шаблон и т.д.).
- **Ключевые точки кода:** `Configuration.Kanban_for_1C/.../DataProcessors/Канбан_Доска_HTML/ManagerModule.bsl` (сборка DOM и JSON в разметку), форма **`Форма.Доска`** — `КанбанДоскаПриНажатии`, оповещение **`ОбновитьКанбанДоскуHTML`**, общие модули **`канбан_*`**. Редактор задач: **`editor-html/`** + макеты справочника **`канбан_Задачи`**.
- Внешний сайт с пользовательской документацией для ответов модели **не нужен**; при задачах «описать функциональность» опирайся на код и **`documentation/`** ниже.

## Связка 1С ↔ страница

- **`window.V8Proxy.fetch(событие, params)`** — запрос из JS в 1С (обработка на форме доски). События см. **`OneKanban-html/src/index.js`** (`changeStatus`, `settingsChanged`, `refresh`, …).
- **`канбан_КанбанДоскаКлиент.ОтправитьОтвет`** → **`V8Proxy.sendResponse`** — ответ 1С в JS (частичное обновление).
- **`Оповестить("ОбновитьКанбанДоскуHTML", …)`** — полная пересборка HTML или точечное обновление через ответ.

## HTML/JS (ограничения проекта)

- Без сторонних UI-библиотек; простой совместимый с WebKit синтаксис. Webpack в `OneKanban-html` — только инлайн-сборка, не фреймворки.

## BSL и стандарты платформы

- Ориентир по стилю и правилам кода: **https://v8std.ru/** — стандарты и связка с **диагностиками** (по коду проверки искать правило и способ исправления).
- В модулях соблюдать принятый стиль: области, **`// @strict-types`**, серверные границы, типовые приёмы БСП там, где уже используются.

## Проверки BSL: EDT и MCP

- В **1C:EDT** включены проверки конфигурации / Sonar BSL — перед завершением задачи по BSL убеждайся, что новые замечания не вводишь (или исправляй осмысленно).
- Подключён **MCP «EDT MCP Server»** (`user-EDT MCP Server`): после правок модулей при необходимости вызывай инструменты **`get_project_errors`**, **`get_problem_summary`**, для расшифровки кода проверки — **`get_check_description`** (по `checkId`), при необходимости **`revalidate_objects`**. Используй их, чтобы соответствовать тем же правилам, что видит разработчик в EDT.

## Документация внутри репозитория (нужно уметь менять)

- Каталог **`documentation/`** — сайт на **Docusaurus**. Типичные правки: **`documentation/docs/`** (Markdown), **`documentation/sidebars.js`**, изображения **`documentation/static/`**, иногда **`documentation/docusaurus.config.js`**. После изменений — сборка по **`npm run build`** из `documentation/` (как у проекта). Демо-страница доски может подтягиваться скриптами из `OneKanban-html` — смотри **`OneKanban-html/scripts/build-demo.js`**.

## Синтакс-помощник и справка платформы

- **EDT:** контекстная справка / синтакс-помощник по объектам и встроенному языку (F1 и панель справки — по настройкам IDE).
- **ИТС:** https://its.1c.ru/ — синтаксис и методы для конкретной ветки **8.3**, если нужна точная сигнатура платформы.

## Обязательность тестов при изменении доски

Если задача меняет поведение или разметку канбана **на стороне 1С** (генерация HTML, данные доски, форма **`Канбан_Доска_HTML`**) или **на стороне фронта** (`OneKanban-html`, макеты шаблонов в расширении), считается **незавершённой**, пока не обновлены или не добавлены соответствующие тесты:

1. **YAXUnit** в конфигурации с тестами (**`Управление_задачами.YAXUNIT`**, при необходимости **`Tests/yaxunit`**).
2. **Веб-тесты Playwright** в **`OneKanban-html/tests/`** (если затронуты селекторы, сценарии UI, фикстуры демо-доски).

Не оставляй только правки кода без согласованных тестов и эталонов.

---

## YAXUnit: как устроено и как модели поддерживать тесты

### Где код тестов

- Общий модуль **`канбан_Тесты`** (`Управление_задачами.YAXUNIT/.../канбан_Тесты/Module.bsl`): процедура **`ИсполняемыеСценарии()`** — реестр наборов и имён тестов; **`ПередВсемиТестами`** — подготовка данных (проекты, статусы, задачи и т.д.). Снимки HTML: **`yaxunit-html-snapshots/test_templates/*.snapshot.txt`** — перед клиентской фазой модуля каталог очищается (**`канбан_ТестыВызовСервера.ОчиститьКаталогСнимковHTMLДляМакетов`** из клиентского **`ПередВсемиТестами`**); внутри одного прогона уже созданный **`*.snapshot.txt`** с тем же именем **не перезаписывается** (второй вызов **`ПроверитьТекстHTMLКанбанДоски`** с тем же именем макета не подменяет файл).
- Серверная обёртка **`канбан_ТестыВызовСервера`** — вызовы с клиента теста на сервер (проверка HTML, изменение данных задач и пр.).
- Серверные эталонные прогоны HTML также есть в **`Tests/yaxunit`** (модуль **`канбан_Тесты`** там короче — не путать полный набор с урезанным).

### Сравнение HTML с макетом (эталоном)

- Процедура **`ПроверитьТекстHTMLКанбанДоски(ИмяМакетаЭталона, ТекстДоски)`** в **`канбан_Тесты`**: при пустом тексте вызывает **`канбан_КанбанДоска.ТекстHTMLКанбанДоски`**, иначе проверяет переданную строку.
- Перед сравнением: **`НормализоватьТекст`** (убирает спец-пробелы, выравнивает переводы строк).
- Из эталона и из факта вырезаются URL вида **`v8config://` + 40 hex** (регулярное выражение в коде), затем **`ЮТест.ОжидаетЧто(ТекстДоски).Равно(ТекстHTMLДоскиЭталон)`** — **полное совпадение строки** после этих шагов.

Когда меняется вывод доски, **эталонный общий макет** с именем вроде **`канбан_ЧетыреПроекта_...`** перезаписывают так, чтобы совпадать с новым корректным HTML.

**Обновление эталонов:** **`projectPath`** в YAXUnit — **корень `Kanban_for_1C`**. Снимки: **`<projectPath>/yaxunit-html-snapshots/test_templates/ИмяМакета.snapshot.txt`** (не в git по умолчанию). Скрипт **`OneKanban-html/scripts/apply-yaxunit-html-snapshots.js`** читает **`Kanban_for_1C/yaxunit-html-snapshots/test_templates/`** и переносит в **`CommonTemplates/.../Template.txt`**; из **`OneKanban-html`**: **`npm run yaxunit:apply-snapshots`**. После переноса в EDT: **обновить ресурсы** проекта **`Управление_задачами.YAXUNIT`** (F5 / «Обновить») или перезапустить EDT. См. **`yaxunit-html-snapshots/README.md`**, **`documentation/docs/Разработка/Сборка-OneKanban-html.md`**.

### Где лежат макеты-эталоны

- **`Управление_задачами.YAXUNIT/src/CommonTemplates/`** — папки **`канбан_*`** с **`Template.txt`** и **`*.mdo`**; имена объектов должны совпадать с строкой **`ИмяМакетаЭталона`** в вызове **`ПроверитьТекстHTMLКанбанДоски`**.
- Новый эталон: создать **общий макет** в дереве EDT (или скопировать соседний **`канбан_*`** и переименовать), добавить ссылку в **`Configuration.mdo`**, если проект этого требует.

### Клиентские тесты доски

- Набор **`канбан_КанбанДоска_Клиент`**: открывают форму **`Обработка.Канбан_Доска_HTML.Форма.Доска`**, выполняют сценарий (клики/оповещения симулируются через серверные процедуры **`канбан_ТестыВызовСервера`**), читают **`ТекстРеквизитаФормыКанбанДоска`** и вызывают **`ПроверитьТекстHTMLКанбанДоски("канбан_ИмяМакета", Реквизит)`**. При новом сценарии: добавить процедуру, зарегистрировать в **`ИсполняемыеСценарии`**, подготовить данные в **`ПередВсемиТестами`** при необходимости, создать макет или переиспользовать существующий.

### Алгоритм для модели при правке доски

1. Определить затронутые тесты: поиск по **`ПроверитьТекстHTMLКанбанДоски`** и именам макетов **`канбан_`**.
2. После изменения кода прогнать тесты в EDT/YAXUnit (перед клиентской фазой **`канбан_Тесты`** очищается **`test_templates`** от **`*.snapshot.txt`**; в прогоне каждый снимок создаётся при первом **`ПроверитьТекстHTMLКанбанДоски`**, повтор с тем же именем файла не перезаписывает); при расхождении эталона — **`npm run yaxunit:apply-snapshots`** из **`OneKanban-html`**, при необходимости удалить нужный **`*.snapshot.txt`** и перегнать тест или обновить **`Template.txt`** вручную.
3. При **новой** функции доски — добавить тест и новый макет (или расширить сценарий и один эталон), не дублировать без нужды.

---

## Веб-тесты (Playwright) в `OneKanban-html`

- Каталог **`OneKanban-html/tests/`**: **`specs/*.spec.js`**, общие **`helpers/board-helper.js`**, фикстуры **`tests/fixtures/`** (например данные для **`four-projects`**).
- Доска открывается через хелперы с подстановкой **`V8Proxy`** и **`sendResponse`** для имитации ответов 1С.
- Если изменились **классы**, **структура DOM**, **число колонок/карточек**, **тексты статусов**, поведение поиска/фильтров/темы — обнови соответствующие **`*.spec.js`** и при необходимости фикстуры; добавь спеки для нового поведения.
- Запуск: из **`OneKanban-html`** обычно **`npm test`** или **`npx playwright test`** (смотри **`package.json`** проекта).

Итог: любое изменение доски сопровождай **парой проверок — YAXUnit (эталон HTML в макетах + логика в BSL) и при затронутом фронте — Playwright в `OneKanban-html/tests`**.
